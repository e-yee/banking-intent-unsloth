model:
  load_in_4bit: False
  max_seq_length: 2048
  dtype: null

tokenizer:
  return_tensors: pt
  padding: True
  truncation: True
  max_length: 2048

generate:
  max_new_tokens: 2
  use_cache: True